# racing-game

This project uses `index.html` as the main entrypoint (Three.js racing demo).

Note: `game.js` was removed because it conflicted with the inline app in `index.html`.
Open `index.html` in a browser to run the demo. Ensure `car.glb` is present in the same folder.